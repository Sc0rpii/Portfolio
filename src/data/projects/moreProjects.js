// Future projects use the same shape as professionalProjects and personalProjects:
// id, img, width, height, title, kind, descriptionKey, overviewKey and url.
// `kind` can be "professional" or "personal" and controls the detail-page badge,
// focus copy and return link. Text values belong in languages/{it,eng}.json.
export const moreProjects = [];
